instalaciones 
pip install streamlit pandas psycopg2-binary

correr como dependencia de phyton
python -m streamlit run app.py

para que corra bien cambie su contraseña de postgrest en el archivo db/conexionDB.py
